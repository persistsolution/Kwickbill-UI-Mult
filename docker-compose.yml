version: "3.8"
services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5176:5176"
    environment:
      - HOST=0.0.0.0
      - PORT=5176
    volumes:
      - .:/app
      - /app/node_modules
