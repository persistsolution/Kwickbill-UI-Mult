version: "3.8"
services:
  react-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5175:5175"
    environment:
      - HOST=0.0.0.0
      - PORT=5175
    volumes:
      - .:/app
      - /app/node_modules
